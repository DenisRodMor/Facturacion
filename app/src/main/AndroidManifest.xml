<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.facturacion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InicioDeSesionActivity"
            android:label="Iniciar Sesion"> <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity
            android:name=".ListaDePagosMIsClientesActivity"
            android:label="Facturas Abonadas" />
        <activity
            android:name=".PagoDeFacturasActivity"
            android:label="Pago de factura" />
        <activity
            android:name=".MisFacturasActivity"
            android:label="Info" />
        <activity
            android:name=".ListaFacturasActivity"
            android:label="Facturas pendientes" />
        <activity
            android:name=".AgregarFacturasActivity"
            android:label="Datos de la factura" />
        <activity
            android:name=".ActualizarDatosActivity"
            android:label="Editar Cliente" />
        <activity
            android:name=".MiClienteActivity"
            android:label="Datos Del Cliente" />
        <activity
            android:name=".ListaDeClientesActivity"
            android:label="Clientes" />
        <activity
            android:name=".AgregarClienteActivity"
            android:label="Agregar Nuevo Clientes" />
        <activity
            android:name=".MainActivity"
            android:label="Sistema de Facturacion">
        </activity>
    </application>

</manifest>